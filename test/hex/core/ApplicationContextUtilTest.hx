package hex.core;

import hex.unittest.assertion.Assert;
//import hex.core.ApplicationContextUtil;
/**
 * ...
 * @author Francis Bourre
 */
class ApplicationContextUtilTest
{
	
	@Test( "test validate Name" )
	public function testValidateName() : Void
	{
		/*Assert.isTrue(ApplicationContextUtil.isValidName("d"), "one letter");
		Assert.isTrue(ApplicationContextUtil.isValidName("isValid"), "many letters with capital");
		Assert.isTrue(ApplicationContextUtil.isValidName("_"), "one underscore");
		Assert.isTrue(ApplicationContextUtil.isValidName("__"), "two underscores");
		Assert.isTrue(ApplicationContextUtil.isValidName("___"), "three underscores");
		Assert.isTrue(ApplicationContextUtil.isValidName("_stillValid"), "start with underscore");
		Assert.isTrue(ApplicationContextUtil.isValidName("still_valid"), "underscore in the middle");
		Assert.isTrue(ApplicationContextUtil.isValidName("AA"), "only captials");
		Assert.isTrue(ApplicationContextUtil.isValidName("Abc100"), "letters ans numbers");

		Assert.isFalse(ApplicationContextUtil.isValidName("aa aa"), "with space");
		Assert.isFalse(ApplicationContextUtil.isValidName(" aaaa"), "start with space");
		Assert.isFalse(ApplicationContextUtil.isValidName("$"), "dollar");
		Assert.isFalse(ApplicationContextUtil.isValidName("0"), "number");
		Assert.isFalse(ApplicationContextUtil.isValidName("dd.dd"), "contains dot");
		Assert.isFalse(ApplicationContextUtil.isValidName(".aa"), "start with dot");
		Assert.isFalse(ApplicationContextUtil.isValidName("-notvalid"), "start with dash");
		Assert.isFalse(ApplicationContextUtil.isValidName("still-notValid"), "with dash in the middle");
		Assert.isFalse(ApplicationContextUtil.isValidName("0aa"), "start with number");
		Assert.isFalse(ApplicationContextUtil.isValidName("0_"), "start with number then underscore");
*/
	}
}