@context( name = 'applicationContext' )
{
	s = "test";
	a = new Array<String>( s, 'yes' );
	
	mapping = mapping( {fromType: "hex.mock.Interface", toClass: hex.mock.Clazz, withName: "anotherID"} );
	
	mappings = new Array<hex.di.mapping.MappingDefinition>
	(
		mapping( {fromType: "String", toValue: s} ),
		mapping( {fromType: "hex.mock.Interface", toClass: hex.mock.Clazz, withName: "id"} )
	);

	owner = new hex.mock.MixedDependenciesOwner( a, mapping, mappings, s );
}